<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Items</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right, #4facfe, #00f2fe);
        }

        .createAccountForUser {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
            width: 350px;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        h3 {
            margin-bottom: 20px;
            color: #333;
            font-weight: 600;
        }

        .createAccountForUser div {
            margin-bottom: 15px;
            text-align: left;
        }

        label {
            font-weight: 400;
            color: #555;
        }

        input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            outline: none;
            transition: 0.3s;
        }

        input:focus {
            border-color: #4facfe;
            box-shadow: 0px 0px 5px rgba(79, 172, 254, 0.5);
        }

        .btn {
            width: 100%;
            background: #4facfe;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s;
        }

        .btn:hover {
            background: #3a9be2;
            transform: scale(1.05);
        }

        @media (max-width: 400px) {
            .createAccountForUser {
                width: 90%;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="createAccountForUser">
        <h3>Add Item</h3>
        <form id="create" method="post" action="/shopkeeper/<%= shopkeeperId %>/additem">
            <div class="c-name">
                <label for="name">Image URL : </label>
                <input type="text" id="name" name="u[i_img]" placeholder="Enter img URL" required>
            </div>

            <div class="c-email">
                <label for="email">Item Name : </label>
                <input type="text" id="email" name="u[i_name]" placeholder="Enter Name" required>
            </div>

            <div class="c-pass">
                <label for="pass">Price : </label>
                <input type="number" id="pass" name="u[i_price]" placeholder="Enter Price" required>
            </div>

            <div class="c-num">
                <label for="num">Globle Name : </label>
                <input type="text" id="num" name="u[i_globle_name]" placeholder="Enter Globle Name" required>
            </div>
                
            <div class="c-loc">
                <label for="loc">Brand Name : </label>
                <input type="text" id="loc" name="u[i_brand_name]" placeholder="Enter Brand Name" required>
            </div>

            <div class="c-btn">
                <button type="submit" class="btn">Add Item</button>
            </div>
        </form>
    </div>
</body>
</html>
